탭메뉴 만들때  js 코드 짜기시작할때 function 안에다 넣고 addeventlistener 넣게되면
코드 function 한번만 실행되기때문에 
클릭을 한번만 클릭한거 받을수있음
function 벗겨내고 add eventlestener 로 시작해야함

탭메뉴 만들때 함수를 더이상 진행하지않고 종류해야할때 return 필요할때가 있음 잘생각..
디버깅할때 엔진에서 코드 순서 어떻게 흘러가는지 생각하면서 디버깅

while (!elem.classList.contains("m-menu-list__down")) {
      elem = elem.parentNode;
      if (elem.nodeName == "BODY") {
        return;
      }
이벤트 버블링에서 
elem = elem.parentNode;
elem = e.target.parentNode; e.target으로하면 하드코딩으로 되어서 while문에 따라 값이 변동되지않음 (유의!)

